#contestant-block
  #contestant-heading
    %h2 The Contestants
  .clear

  #contestant-photos
    - @entries.each do |entry|
      .photobox
        = image_tag entry.profile_photo.url(:masonry)
        .artist= entry.stage_name
