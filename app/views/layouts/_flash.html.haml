.flash
  - if notice.present?
    .notice
      = notice
      %a.close{:href => "#"}
  - if alert.present?
    .alert
      = alert
      %a.close{:href => "#"}
  -#- if defined?(resource) && resource.errors.present?
  -#  .alert
  -#    The form could not be saved.
  -#    %a.close{:href => "#"}