<div class="heading">
  <h2>Log in</h2>
</div>

<%= simple_form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
    <%= f.input :email %>
    <%= f.input :password %>
    <%= f.submit "Log in" %>
<% end %>