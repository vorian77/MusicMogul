<div class="heading">
  <h2>Mogul Sign Up</h2>
</div>

<%= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
    <%= f.input :email %>
    <%= f.input :password %>
    <%= f.submit "Sign up" %>
<% end %>